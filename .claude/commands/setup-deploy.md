---
description: Bootstrap project infrastructure via infrastructure-setup skill; follow up with deploy-pipeline for CI/CD
allowed-tools:
  - Skill
---

# Instructions

Use the `infrastructure-setup` skill. For CI/CD setup after bootstrap, follow up with the `deploy-pipeline` skill.
