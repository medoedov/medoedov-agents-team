---
status: planned                    # planned -> in_progress -> done
depends_on: []                     # номера задач-зависимостей
wave: 1                            # волна параллельного выполнения
skills: [code-writing]             # МАССИВ скиллов для загрузки
verify: []                         # типы верификации: smoke, user (опционально)
reviewers: [code-reviewer, security-auditor, test-reviewer]  # явно указать. Пусто = fallback на дефолтные три
teammate_name:                     # optional human-readable worker label for logs/reports only; no routing, role, or model-binding semantics
---

# Task N: Название

## Required Skills

Перед выполнением задачи загрузи:
- `/skill:code-writing` — [skills/code-writing/SKILL.md](.claude/skills/code-writing/SKILL.md)

## Description

Что делаем и зачем. (Столько, сколько нужно для понимания — без ограничения длины.)

## What to do

Конкретные шаги — ЧТО, не КАК. Не псевдокод.

## TDD Anchor

<!-- Fill if task includes writing code. For non-code tasks (user instructions, deploy, config) — delete this section. -->

Тесты, которые нужно написать ДО реализации. Пишем → запускаем → убеждаемся что падают → пишем код → убеждаемся что проходят.

- `tests/test_api.py::test_create_user` — POST /api/users создаёт пользователя, возвращает 201
- `tests/test_api.py::test_create_user_duplicate` — POST с существующим email → 409

## Acceptance Criteria

- [ ] Критерий 1
- [ ] Критерий 2

## Context Files

- [user-spec.md](../user-spec.md)
- [tech-spec.md](../tech-spec.md)
- [decisions.md](../decisions.md)
- [architecture.md](.claude/skills/project-knowledge/references/architecture.md)
- [patterns.md](.claude/skills/project-knowledge/references/patterns.md)
- [project.md](.claude/skills/project-knowledge/references/project.md)
- [engineering-principles.md](.claude/skills/project-knowledge/references/engineering-principles.md)
- [другие файлы по необходимости — БД, API, деплой, конкретные файлы кода]

## Verification Steps

### Automated
<!-- Tests to run. Always present for code tasks. -->
- `pytest tests/test_xxx.py -v` → all pass

### Smoke
<!-- Executable checks — agent runs before reviews. No deployment needed.
     From tech-spec Verify-smoke. Types: command (curl, python -c), MCP tool,
     API call, docker build, local server check, agent with test prompt.
     Omit section if no smoke checks. -->

### User
<!-- Agent asks user to verify (UI, behavior, experience).
     From tech-spec Verify-user. Omit section if no user checks. -->

## Details

<!-- All details for task execution — technical, organizational, any other. -->

**Files:** путь/к/файлу.ts — что сделать
**Dependencies:** другие задачи, пакеты
**Edge cases:** список
**Implementation hints:** подсказки (НЕ псевдокод)

## Reviewers

Canonical report path for every reviewer and round:
`logs/working/task-{task-id}/{reviewer}-round{N}.json`.

The parent/root orchestrator schedules these reviewers under available slots and aggregates
their reports. The task executor and reviewers do not perform nested fan-out.

- **code-reviewer** (`{reviewer}` = `code-reviewer`)
- **security-auditor** (`{reviewer}` = `security-auditor`)
- **test-reviewer** (`{reviewer}` = `test-reviewer`)

## Post-completion

- [ ] Записать краткий отчёт в decisions.md по шаблону (Summary: 1-3 предложения, ревью со ссылками на JSON, без таблиц файндингов и дампов)
- [ ] Если отклонились от спека — описать отклонение и причину
- [ ] Обновить user-spec/tech-spec если что-то изменилось
- [ ] Записать verification/reviewer evidence и `final_status: done` в `{task-id}.run.yml`
- [ ] Не создавать и не обновлять checkbox задачи в tech-spec; статус хранится в frontmatter задачи и run.yml
