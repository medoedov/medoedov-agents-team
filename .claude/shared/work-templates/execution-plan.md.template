---
schema_version: 1
feature: "{feature}"
# SHA-256 covers the canonical Markdown plan payload below this frontmatter.
# The mutable execution_approval block is excluded from that digest.
plan_payload_sha256_scope: markdown_body_below_frontmatter
execution_approval:
  status: pending            # pending | approved | revoked
  approved_at: null          # ISO-8601 UTC; required when approved
  owner_ref: null            # durable owner identity/authority reference
  approved_plan_sha256: null # 64 lowercase hex; body digest defined above
  auto_continue: false       # semantic owner intent; never phrase matching
  directive_evidence_ref: null
  amendment_gate_manifest_ref: null
  amendment_gate_manifest_sha256: null # exact immutable manifest bytes
  continuation:
    command: /do-all-tasks # /do-task | /do-all-tasks | /write-code
    argv: ["{feature}"]   # normalized strings, no options or reconstruction
    digest: null          # canonical JSON SHA-256 of command + argv
---

# Execution Plan: {Feature Name}

**Создан:** YYYY-MM-DD

---

## Wave 1 (независимые)

### Task 1: [название]
- **Skill:** code-writing
- **Reviewers:** code-reviewer, security-auditor, test-reviewer
- **Verify-smoke:** `curl localhost:3000/api/endpoint` → 200

### Task 2: [название]
- **Skill:** code-writing
- **Reviewers:** code-reviewer, test-reviewer
- **Verify-user:** open localhost:3000/users → form renders

## Wave 2 (зависит от Wave 1)

### Task 3: [название]
- **Skill:** code-writing
- **Reviewers:** code-reviewer, security-auditor

## Проверки, требующие участия пользователя

- [ ] Task 2: пользователь проверяет [что именно]
- [ ] После всех волн: финальное тестирование
